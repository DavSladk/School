#ifndef OEMS_H
#define OEMS_H

#include <mpi.h>
#include <stdio.h>

const size_t NUMBER_SIZE = 1;
const size_t NUMBER_COUNT = 8;
const char* FILE_NAME = "numbers";
const char* FILE_MODE = "rb";

#endif // OEMS_H